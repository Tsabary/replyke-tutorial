import { View, Text } from "react-native";
import React from "react";

const CreateScreen = () => {
  return (
    <View className="bg-aqua-500 flex-1 justify-center items-center gap-4">
      <Text className="text-3xl">Create</Text>
    </View>
  );
};

export default CreateScreen;
